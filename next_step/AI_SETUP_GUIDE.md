# AI 内容生成功能设置指南

## 📋 概述

本文档说明如何设置和配置 AI 内容生成功能（Title & Description Generator）。

---

## 1. 环境变量配置

### 1.1 创建 .env 文件

在 `server/` 目录下创建 `.env` 文件（参考 `server/.env.example`）：

```bash
cd server
cp .env.example .env
```

### 1.2 配置 AI 服务

#### 选项 A：使用 OpenAI（推荐）

1. 访问 [OpenAI Platform](https://platform.openai.com/)
2. 注册账号并创建 API Key
3. 在 `.env` 文件中设置：

```env
AI_PROVIDER=openai
OPENAI_API_KEY=sk-your-api-key-here
```

**成本**：约 $0.01-0.05 每次生成（取决于输入长度）

#### 选项 B：使用 Anthropic Claude

1. 访问 [Anthropic Console](https://console.anthropic.com/)
2. 注册账号并创建 API Key
3. 在 `.env` 文件中设置：

```env
AI_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
```

**成本**：约 $0.008-0.04 每次生成

---

## 2. 安装依赖

确保已安装所有依赖：

```bash
cd server
npm install
```

依赖包括：
- `axios` - HTTP 客户端（用于调用 AI API）
- `express` - Web 框架
- `dotenv` - 环境变量管理

---

## 3. 启动服务器

### 开发模式

```bash
npm run dev
```

### 生产模式

```bash
npm start
```

服务器将在 `http://localhost:3000` 启动。

---

## 4. 测试功能

### 4.1 测试 API 端点

使用 curl 或 Postman 测试：

```bash
curl -X POST http://localhost:3000/api/generate-title-description \
  -H "Content-Type: application/json" \
  -d '{
    "type": "title",
    "currentTitle": "My Website",
    "targetKeyword": "SEO tools",
    "count": 3
  }'
```

### 4.2 测试前端

1. 启动前端服务器（在项目根目录）：
   ```bash
   npm start
   ```

2. 访问 `http://localhost:8080/seo-analyzer.html`

3. 输入一个网站 URL 进行分析

4. 在分析结果中，找到 "Title Tag" 或 "Meta Description" 检查项

5. 点击 "Generate AI Version" 按钮

6. 等待 AI 生成结果（3-5 秒）

---

## 5. 功能限制配置

### 5.1 免费版

- AI 生成次数：0 次/月
- 功能：完全禁用

### 5.2 Starter 计划

- AI 生成次数：10 次/月
- 每次生成版本数：3 个

### 5.3 Professional 计划

- AI 生成次数：100 次/月
- 每次生成版本数：5 个

### 5.4 Enterprise 计划

- AI 生成次数：无限
- 每次生成版本数：5 个

**注意**：当前 MVP 版本暂未实现用户认证和配额限制，所有用户都可以使用。后续版本需要添加用户系统。

---

## 6. 成本控制

### 6.1 缓存机制

系统已实现内存缓存（24 小时）：
- 相同输入会返回缓存结果
- 降低 API 调用成本

### 6.2 生产环境建议

在生产环境中，建议使用 Redis 缓存：

```javascript
// 在 server/services/aiContentGenerator.js 中
// 替换内存缓存为 Redis
const redis = require('redis');
const client = redis.createClient();
```

---

## 7. 故障排除

### 7.1 API Key 错误

**错误信息**：`AI service not configured`

**解决方案**：
1. 检查 `.env` 文件是否存在
2. 确认 API Key 格式正确
3. 确认 API Key 有足够余额

### 7.2 API 超时

**错误信息**：`AI service timeout`

**解决方案**：
1. 检查网络连接
2. 增加超时时间（在 `aiContentGenerator.js` 中）
3. 重试请求

### 7.3 生成失败

**错误信息**：`Failed to generate content`

**可能原因**：
- API 配额用完
- 输入内容过长
- API 服务暂时不可用

**解决方案**：
1. 检查 API 配额
2. 缩短输入内容
3. 稍后重试

---

## 8. 性能优化

### 8.1 响应时间

- **目标**：< 5 秒
- **优化**：
  - 使用缓存
  - 优化 Prompt 长度
  - 使用更快的模型（如 GPT-3.5-turbo）

### 8.2 并发处理

当前实现支持并发请求，但建议：
- 添加速率限制（Rate Limiting）
- 使用队列处理大量请求

---

## 9. 安全注意事项

### 9.1 API Key 安全

- ✅ **不要**将 API Key 提交到 Git
- ✅ 使用 `.env` 文件（已添加到 `.gitignore`）
- ✅ 在生产环境使用环境变量

### 9.2 输入验证

系统已实现基本输入验证：
- URL 格式验证
- 内容长度限制
- XSS 防护（前端）

---

## 10. 下一步

### 10.1 待实现功能

- [ ] 用户认证系统
- [ ] 配额管理
- [ ] Redis 缓存
- [ ] 历史记录
- [ ] 编辑功能

### 10.2 优化方向

- [ ] 更智能的 Prompt 优化
- [ ] 多语言支持
- [ ] 批量生成
- [ ] A/B 测试建议

---

**文档版本**：v1.0  
**创建日期**：2024年  
**最后更新**：2024年

